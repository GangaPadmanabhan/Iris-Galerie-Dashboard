type SeriesRow = { date: string; revenue: number; orders: number };

type Props = {
  series: SeriesRow[];
};

export function DirectionCharts({ series }: Props) {
  // placeholder implementation for build; replace with real charts later
  return null;
}
